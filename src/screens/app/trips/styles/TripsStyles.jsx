import { StyleSheet } from "react-native"
import { scale, verticalScale, moderateScale } from 'react-native-size-matters';

const TripsStyles = StyleSheet.create({
    container: {
        flex: 1,
    }

})

export default TripsStyles